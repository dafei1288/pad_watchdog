# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

家长。在自己家里管理一个或多个孩子的 Pad 观看时间，需要知道"这周还能看多久"，并在计划时间到时被提醒。操作设备手机和电脑都常用，两端都是主要场景。

孩子本人不操作本应用，应用也不安装在被管控的 Pad 上。

## Product Purpose

把孩子的 Pad 观看时间记录清楚并统计出来，以自然周为周期给出额度、剩余和透支情况；家长开始/结束时按一次计时，或事后手动补录。成功 = 家长随时能看到本周还剩多少时间、今天看了多久、哪个孩子看了多久，并且到点会被可靠地提醒。

## Positioning

不锁屏、不装在被管控设备上、不申请任何系统家长控制权限——只做记录与统计。对照的是"系统级家长控制/锁屏工具"：那些要动孩子设备，本应用只需要家长自己的浏览器。

机制上可被别家复制但组合独特：自然周额度 + 单次上限 + 显式透支并从下周额度抵扣（累计封顶）+ 实时倒计时自动结算 + 每日星级评价 + 多孩子同图配色对比。

## Operating Context

- 家长在孩子开始看 Pad 时点"开始观看"（可设本次计划分钟数），看完点"结束观看"；忘了计的就用手动补录。
- 计划时长到点是硬提醒：循环响铃 + 浏览器标题闪烁 + 弹层，点"知道了"才停；会话同时按计划结束时刻自动结算。
- 周期是自然周（周一 00:00 起算），周与周之间靠"预扣/透支"衔接。
- 自部署：Docker 单容器（Express 托管构建产物 + SQLite 单文件持久化），数据留在自己机器上。
- 配置页有单管理密码保护（首次进入时设置）；其余页面无需登录。
- 界面语言中文。

## Capabilities and Constraints

已确认功能：

- 多孩子档案：各自独立的额度、记录、评分与周账本；头像可用 emoji 或上传图片；每个孩子分配固定配色用于图表对比。
- 计时：实时计时器（可设本次计划时长，倒计时到点自动结算）+ 手动补录；每条记录可删除。
- 周额度规则：每周总时长上限（默认 150 分钟）、单次连续观看上限（默认 90 分钟）、累计透支上限（默认 60 分钟），均为每孩子可配置项。
- 超用规则：本周可超用，超用部分从下周额度抵扣，累计透支封顶。
- 每日评价：每天每孩子 1–5 星 + 一句话点评。
- 四个页面：查看（本周剩余、计时、今日评价、补录、本周记录）、日历（月视图用量 + 评分）、统计对比（按周/按日的多孩子柱状图）、配置。
- 服务端 API 保护配置类写操作，读操作公开。

已确认但尚未实现：

- 预约观看时段：从当前时间起预约，形成观看日历并提醒；预约只做日历展示与提醒，不预扣额度，实际使用才计时。需求已确认，代码中还没有。

明确不做：

- 锁屏/强制限制。Web 页面没有这个能力，强制锁屏需要走系统家长控制；本产品不做。
- 多家庭/多租户账号体系。定位是自家使用，单管理密码即可。

技术约束：存储为 SQLite 单文件（`node:sqlite`，默认 `data/pad-watchdog.db`），前后端在同一仓库内，前端构建产物由 Express 托管。

## Brand Commitments

- 现有名称：**Pad 时间管家**（应用标题）；仓库名 `pad-watchdog`。
- 界面用中文，语气是家长对自己说话，不用营销腔。
- 孩子身份用头像（emoji 或上传图片）+ 固定颜色表达，颜色是跨图表识别孩子的主要手段。

## Evidence on Hand

- `docs/req.md`：需求与已确认决策表，是现有产品记录。
- 运行中的实现：`src/views/*`（四个页面 + 登录）、`server/index.js`（HTTP API）、`server/db.js`（SQLite 模式与周结算）、`src/rules.js`（纯函数规则引擎）、`tests/*.test.js`。
- 没有真实用户数据、证言、案例或指标。后续工作不得编造使用量、用户评价或效果声明。

## Product Principles

1. 只记录，不控制。产品让家长看见用量并按时提醒，执行留给人；任何需要动孩子设备的能力都出界。
2. 规则必须可解释。额度、单次上限、透支封顶都是能一句话讲清的算术，并且每个孩子可配置。
3. 预先结算，事后可查。周与周用账本衔接，跨周会话按重叠分钟拆分，统计口径与展示时长一致。
4. 提醒要可靠到刺耳。到点提醒是产品仅有的"强制"手段，必须循环、显眼、需要人确认才停。
5. 数据留在自家。单机自托管、单文件存储，不引入账号、云服务或外部依赖。
